# Corezoid Prolog Example

The project requires custom build with import dependencies.
To do so specify build script in Corezoit Gitcall node.
#### Build command:
```
swipl -g "pack_install(matrix, [interactive(false)])."
```
